{
    "name": "project-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix reservation-app\"",
        "dev:backend": "npm run dev --prefix backend",
        "dev:frontend": "npm run start --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^7.6.0",
        "eslint": "^8.33.0",
        "prettier": "^2.8.1"
    },
    "dependencies": {
        "date-fns": "^4.1.0",
        "jsonwebtoken": "^9.0.2",
        "moment-timezone": "^0.5.46"
    }
}
